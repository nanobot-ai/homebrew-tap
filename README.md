# Obot Tap

## How do I install these formulae?

`brew install nanobot-ai/tap/<formula>`

Or `brew tap nanobot-ai/tap` and then `brew install <formula>`.


## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
